# 분석툴 고도화 마스터플랜

**목표**: 통계 결과 산출 도구 → 공공기관 보고서 수준의 해석·시각화·보고 자동화 도구로 고도화.
**원칙**: 신규 런타임 의존성 0개 (순수 TypeScript 구현 유지). TDD. 태스크당 커밋 1개.
**실행 모델**: 이 문서는 Fable 5가 작성한 계획이며, 실제 구현은 Sonnet 5 세션이 수행한다.

---

## 구현자(Sonnet 5) 실행 규칙 — 반드시 준수

1. **에이전트 시스템**: 모든 작업 전 `python C:/업무/agents/scripts/update_status.py <id> working "..."` 선언, 완료 시 `done`. 로직 작업은 `backend`, UI 작업은 `frontend`, 검증은 `tester-unit`, 리드는 `lead-dev`. 지휘 체계: orchestrator → lead-dev → 담당.
2. **한자·일본어 절대 금지**: 코드·주석·커밋 메시지·파일명 전부. "분석"은 순수 한글(U+BD84 U+C11D)로만.
3. **TDD**: 태스크마다 실패하는 테스트 먼저 작성 → 구현 → 통과 확인 → 커밋. 커밋 메시지에 `Co-Authored-By: Claude <noreply@anthropic.com>` 포함.
4. **Bash 작업 디렉터리 리셋 주의**: 매 Bash 호출 후 cwd가 `C:\Users\lhkau`로 초기화된다. 모든 저장소 명령은 `cd "C:/업무/분석툴/.worktrees/analytics-engine-expansion" && ...` 형태로 명시적 cd 체인 필수.
5. **작업 위치**: 반드시 워크트리 `C:\업무\분석툴\.worktrees\analytics-engine-expansion` (브랜치 `feature/analytics-engine-expansion`)에서만 작업. **master에는 계획과 무관한 미커밋 변경(공유링크 API 서버화·RLS)이 있으므로 master 직접 수정 금지.**
6. **검증**: 태스크마다 `npx vitest run` 전체 통과, Phase 종료마다 `npm run build` 성공. 실행 증거 없이 완료 선언 금지. 최종 검증은 배포 URL(`policy-analytics-engine.vercel.app`) 기준.
7. **실행 방식**: superpowers:subagent-driven-development로 태스크 단위 위임 권장. 탐색성 서브에이전트는 `model: "haiku"`.

---

## 현재 상태 스냅샷 (2026-07-06 기준)

| 항목 | 상태 |
|---|---|
| master | `6888be0` + **미커밋 변경** (app/api/shares/·app/api/comments/·lib/data/shareClient.ts·shareMutations.server.ts·shareServer.ts·lib/supabase/admin.ts·store/supabaseStore/ShareLinkButton/CommentSection/shared page/package.json + supabase/migrations/0004_harden_project_sharing_rls.sql) — 계획 범위 밖, Phase 6에서 별도 커밋 |
| 워크트리 | `.worktrees/analytics-engine-expansion` @ `9fa30a5` (Task 1 완료: `a9b14ce` statUtils.ts + `9fa30a5` 테스트 보강) |
| 테스트 | 118/118 통과 기준선 |
| 상세 확장 플랜 정본 | `C:\업무\분석툴\docs\superpowers\plans\2026-07-06-분석엔진-확장.md` (master 경로에 untracked로 존재, 워크트리 브랜치에는 없음 → **master 경로에서 읽을 것**) |
| 스택 | Next.js 16.2.10 App Router · React 19.2.4 · Recharts ^3.9.2 · Astryx UI · Supabase · Vitest ^4.1.9 · Vercel |
| 완료된 이전 계획 | 호버/매뉴얼/샘플/테스트 계획(quizzical-marinating-wigderson.md)은 커밋 `c75e130`으로 **이미 완료** — 재작업 금지 |

---

## Phase 0 — 재개 검증 (구현 착수 전 필수)

**Step 0-1. 워크트리 상태 확인**
```bash
cd "C:/업무/분석툴/.worktrees/analytics-engine-expansion" && git log --oneline -3 && npx vitest run
```
기대: HEAD `9fa30a5`, 118/118 통과. 불일치 시 중단 후 사용자 보고.

**Step 0-2. Task 1 품질 재리뷰 마무리** (이전 세션에서 API 오류로 중단됨)
- `git show 9fa30a5 --stat`으로 테스트 파일만 변경됐는지 확인
- 추가된 두 케이스 검증: `chiSquarePValue(1, 10) ≈ 0.9999`, `chiSquarePValue(9, 10) ≈ 0.53` — 둘 다 급수(series) 분기 진입 (s=5일 때 x=0.5/4.5 < s+1=6)
- 이전 리뷰의 Minor 이슈 2건은 비차단으로 판정 완료 — **재론 금지**
- 문제 없으면 Task 1 종결, Task 2로 진행

---

## Phase 1 — 분석 엔진 확장 (Tasks 2~11)

**정본은 `docs/superpowers/plans/2026-07-06-분석엔진-확장.md`** — 태스크별 상세 스펙·테스트 케이스·커밋 메시지가 전부 그 문서에 있다. 이 마스터플랜과 충돌 시 정본이 우선. 남은 태스크:

| # | 파일 | 내용 |
|---|---|---|
| 2 | groupComparator.ts | 리팩터 + Cohen's d 효과크기 |
| 3 | groupComparator.ts | eta 제곱 (ANOVA) |
| 4 | regressionUtils.ts | 신규 생성 (선형회귀 유틸) |
| 5 | correlationAnalyzer.ts | 수치쌍 p-value·유의성 판정 |
| 6 | correlationAnalyzer.ts | 범주형 카이제곱 + Cramér's V |
| 7 | correlationAnalyzer.ts | VIF (다중공선성) |
| 8 | outlierDetector.ts | Z-score 병합 재작성 (고신뢰/참고 이상치 구분) |
| 9 | dateDetector.ts | MoM/YoY 변화율 |
| 10 | index.ts | 통합 배선 + 통합 테스트 |
| 11 | — | 전체 검증 및 최종 리뷰 |

**Phase 1 완료 후 타입 계약** (Phase 2~4 코드가 이 계약을 전제로 작성됨):

```typescript
export interface EffectSize {
  type: "cohen_d" | "eta_squared";
  value: number;
  magnitude: "작음" | "중간" | "큼";
}
// GroupComparisonResult에 추가: effectSize: EffectSize | null; interpretation: string;
// CorrelationPair에 추가: pValue: number; significant: boolean; interpretation: string;
export interface CategoricalCorrelationPair {
  columnA: string; columnB: string;
  chiSquare: number; pValue: number; significant: boolean;
  cramersV: number; reliable: boolean; interpretation: string;
}
export interface VifResult { column: string; vif: number | null; concern: boolean; }
// OutlierColumnInfo에 추가: highConfidenceIndices: number[]; referenceIndices: number[];
// TimeSeriesAnalysis에 추가: momChange: number | null; yoyChange: number | null;
// AnalysisResult에 추가: categoricalCorrelationSummary?: CategoricalCorrelationPair[]; vifSummary?: VifResult[];
```

---

## Phase 2 — 통계 시각화 고도화 (4 태스크)

### Task 2.1 — 산점도 + 추세선

**타입 확장** (`types/analysis.ts`):
```typescript
export type ChartType = "bar" | "line" | "pie" | "grouped-bar" | "scatter";
export interface ChartSpec {
  // 기존 필드 유지, 추가:
  subtitle?: string;
  trendLine?: { slope: number; intercept: number } | null;
  errorKey?: string;
}
```

**로직** (`lib/analytics/chartRecommender.ts`): `buildScatterSpec(dataset, pair)` 신규.
- 두 컬럼이 모두 숫자인 행만 수집해 점 배열 생성. 3점 미만이면 null 반환
- `SCATTER_MAX_POINTS = 300` 초과 시 균등 간격 샘플링: `const step = Math.max(1, Math.ceil(points.length / 300))`
- 단순 선형회귀: `slope = sxy / sxx`, `intercept = meanY - slope * meanX`. `sxx === 0`이면 `trendLine: null`
- id: `scatter-${columnA}-${columnB}`, subtitle: `상관계수 X.XX, p=0.XXXX (유의)` 형식
- `recommendCharts`에 6번째 매개변수 `correlationSummary?: CorrelationPair[]` 추가. 반환 직전: significant 쌍만 필터 → |coefficient| 내림차순 정렬 → 최상위 쌍의 scatter spec을 push
- `lib/analytics/index.ts`의 `recommendCharts` 호출에 `correlationSummary` 전달

**렌더러** (`components/render/ChartRenderer.tsx`): pie 분기 앞에 scatter 분기 추가.
```tsx
) : spec.type === "scatter" ? (
  <ScatterChart>
    <CartesianGrid strokeDasharray="3 3" stroke="#e5e7eb" />
    <XAxis type="number" dataKey={spec.xKey} domain={["auto", "auto"]} tick={{ fontSize: 12 }} name={spec.xKey} />
    <YAxis type="number" dataKey={spec.yKey} domain={["auto", "auto"]} tick={{ fontSize: 12 }} name={spec.yKey} />
    <Tooltip cursor={{ strokeDasharray: "3 3" }} />
    <Scatter data={spec.data} fill="#3b82f6" fillOpacity={0.7} />
    {spec.trendLine ? (
      <ReferenceLine segment={buildTrendSegment(spec)} stroke="#ef4444" strokeDasharray="6 4" strokeWidth={1.5} />
    ) : null}
  </ScatterChart>
) : (
```
`buildTrendSegment(spec)`: data에서 x 최소·최대를 구해 `[{x: minX, y: slope*minX+intercept}, {x: maxX, y: slope*maxX+intercept}]` 반환. Recharts import에 `Scatter, ScatterChart, ReferenceLine` 추가.

**테스트** (chartRecommender.test.ts 확장): 30행 광고비/매출액 데이터셋 (`매출액: i*25 + (i%3)`) →
- scatter 타입 spec이 존재한다
- `trendLine?.slope`가 25에 근사 (오차 ±1)
- subtitle에 "상관계수" 포함
- `data.length ≤ 300`

**커밋**: `feat: 유의한 상관쌍 산점도·추세선 차트 추가`

### Task 2.2 — 상관 행렬 히트맵

**로직** (`lib/analytics/heatmapMatrix.ts` 신규 — 순수 로직, 테스트 대상):
```typescript
export interface HeatmapCell { coefficient: number; significant: boolean; }
export interface CorrelationMatrix {
  columns: string[];
  cells: Record<string, Record<string, HeatmapCell | null>>;
}
export function buildCorrelationMatrix(pairs: CorrelationPair[]): CorrelationMatrix
```
- columns: 쌍에 등장하는 고유 컬럼명. 3개 미만이면 `{ columns: [], cells: {} }` (히트맵 생략)
- 대각선: `{ coefficient: 1, significant: false }`
- 대칭 채움 (A×B = B×A), 없는 쌍은 null

**테스트**: 대칭성·단위 대각선·누락 쌍 null / 변수 3개 미만 → 빈 columns.

**UI** (`components/render/CorrelationHeatmap.tsx` 신규, "use client", 테이블 기반):
- 셀 배경: 양수 `rgba(239,68,68,α)`, 음수 `rgba(59,130,246,α)`, `α = Math.min(0.85, Math.abs(c) * 0.85)`
- |c| > 0.55이면 흰색 텍스트
- significant 셀에 `*` 표기, 하단 각주 "* p<0.05 통계적으로 유의"

**통합**: `AnalysisDashboardTabs.tsx`의 상관 SimpleDataTable(약 271행) 위에 히트맵 배치.

**커밋**: `feat: 상관 행렬 히트맵 추가`

### Task 2.3 — 그룹비교 오차막대

- `GroupComparisonResult.groupMeans` 항목에 `sd: number` 추가 — 표본 표준편차 (n-1 분모): `values.length < 2 ? 0 : Math.sqrt(합((v-mean)²) / (n-1))`. 테스트: `[2,4,6]` → sd 2.0
- chartRecommender: 첫 significant 그룹비교에 대해 bar spec push — `xKey: "group"`, `yKey: "mean"`, `errorKey: "sd"`, data는 `{ group, mean: Number(mean.toFixed(2)), sd: Number(sd.toFixed(2)) }`, subtitle = interpretation
- ChartRenderer: `ErrorBar` import, errorKey 존재 시 `<Bar>` 내부에 `<ErrorBar dataKey={spec.errorKey} width={4} strokeWidth={1.5} stroke="#6b7280" />`

**커밋**: `feat: 그룹비교 평균 오차막대 차트 추가`

### Task 2.4 — MoM/YoY 대시보드 표시

- `AnalysisDashboardTabs.tsx`의 `TIME_SERIES_COLUMNS`에 momChange/yoyChange 컬럼 추가
- 포맷: null → "산출 불가", 그 외 `(v > 0 ? "+" : "") + v.toFixed(1) + "%"`

**커밋**: `feat: 시계열 요약에 전월·전년동월 대비 변화율 표시`

---

## Phase 3 — AI 내러티브 고도화 (Task 3.1)

**타입** (`types/analysis.ts`): `NarratorInput`에 선택 필드 추가 (원본 행 배제 원칙 유지 — points 제외):
```typescript
export interface NarratorTimeSeries {
  dateColumn: string; numericColumn: string;
  trendDirection: string;
  momChange: number | null; yoyChange: number | null;
}
// NarratorInput에 추가:
//   correlationSummary?: CorrelationPair[];
//   categoricalCorrelationSummary?: CategoricalCorrelationPair[];
//   vifSummary?: VifResult[];
//   groupComparisonSummary?: GroupComparisonResult[];
//   timeSeriesSummary?: NarratorTimeSeries[];
```

**프롬프트** (`lib/ai/prompts.ts`): 규칙 기반 인사이트 섹션 앞에 조건부 섹션 4개 추가:
1. `## 상관 검정 결과` — `상관계수 X.XX, p=0.XXXX, 유의함/유의하지 않음`
2. `## 그룹 차이 검정 결과` — interpretation + `효과크기 d=X.XX (magnitude)` 또는 `eta 제곱=X.XX (magnitude)`
3. `## 다중공선성 경고` — concern인 VIF만 `- 컬럼: VIF X.X (10 초과)`
4. `## 시계열 변화율` — 추세·전월대비·전년동월대비, null은 "산출 불가"

시스템 프롬프트에 한 줄 추가:
```
- 통계적 유의성과 효과크기를 함께 서술합니다. 표본이 커서 유의하더라도 효과크기가 작으면 "실질적 차이는 크지 않다"고 명시합니다.
```

**배선**:
- `components/AiNarratorPanel.tsx`의 `buildNarratorInput`에서 5개 필드 매핑 (timeSeries는 5필드로 축약)
- **함정 (필수)**: `app/api/narrate/route.ts`의 `pickNarratorInput`은 필드 화이트리스트 방식 — 5개 필드명을 화이트리스트에 추가하지 않으면 조용히 누락된다.

**테스트** (`lib/ai/prompts.test.ts` 신규): 포함 4케이스 ("p=", "효과크기", "다중공선성", "전월대비" 각 문자열 존재) + 필드 미제공 시 해당 섹션 부재 1케이스.

**커밋**: `feat: AI 내러티브에 유의성·효과크기·다중공선성·변화율 반영`

---

## Phase 4 — 보고서 고도화

### Task 4.1 — 보고서 신규 섹션 (`lib/report/buildReport.ts`)

"## 규칙 기반 종합 인사이트" 앞에 조건부 섹션 추가:
1. `## 상관 검정` — significant 쌍의 interpretation 불릿
2. `## 범주형 변수 연관성` — significant 또는 `!reliable`(기대빈도 부족 경고) 항목
3. `## 다중공선성 진단` — concern VIF 컬럼 나열 + "회귀 분석 시 해당 변수들의 동시 투입에 주의가 필요합니다." 문장
4. `## 그룹 차이 검정` — interpretation 불릿
5. `## 이상치 탐지` — `고신뢰 이상치 N건, 참고 수준 N건 (정상 범위 lower ~ upper)`
6. `## 시계열 동향` — 추세 + MoM/YoY 부호 포함 % (null → "산출 불가")

**테스트** (`lib/report/buildReport.test.ts` 신규): 픽스처로 섹션 제목 + "p="/"효과크기"/"고신뢰" 문자열 존재 확인, 필드 undefined 시 섹션 부재 확인.

**커밋**: `feat: 보고서에 유의성·효과크기·이상치 신뢰도·변화율 섹션 추가`

### Task 4.2 — 인쇄/PDF 저장

- `lib/report/exportHtml.ts` style 블록에 추가:
```css
@media print {
  body { background: #fff; color: #000; font-size: 11pt; }
  h1, h2 { break-after: avoid; }
  table, li { break-inside: avoid; }
}
@page { margin: 18mm; }
```
- `ReportView.tsx`에 "인쇄 / PDF 저장" 버튼 (`onClick={() => window.print()}`) — 기존 내보내기 버튼과 동일한 Astryx 컴포넌트 사용

**커밋**: `feat: 보고서 인쇄·PDF 저장 지원`

---

## Phase 5 — Web Worker 성능 개선 (Task 5.1)

**신규** `lib/analytics/analysisWorker.ts`:
```typescript
self.onmessage = (event) => {
  try {
    postMessage({ ok: true, result: runAnalysis(event.data) });
  } catch (error) {
    postMessage({ ok: false, message: error instanceof Error ? error.message : String(error) });
  }
};
```

**신규** `lib/analytics/runAnalysisAsync.ts`:
- `typeof Worker === "undefined"` → 동기 폴백 `Promise.resolve(runAnalysis(dataset))`
- 그 외: `new Worker(new URL("./analysisWorker.ts", import.meta.url))` → onmessage에서 terminate 후 ok면 resolve, 아니면 reject → **onerror는 terminate 후 동기 폴백으로 resolve** (기능 상실보다 UI 일시 정지가 낫다)

**교체**: `app/(app)/upload/page.tsx:203`의 `runAnalysis(cleaned)` → `await runAnalysisAsync(cleaned)` (호출부 async 보장).

**검증**: vitest는 폴백 경로만 테스트 (node에 Worker 없음). Worker 경로는 `npm run dev` + 대용량 샘플 업로드로 수동 확인.

**커밋**: `perf: 분석 파이프라인 Web Worker 이전`

---

## Phase 6 — 통합 검증·머지·배포

1. **전체 구현 코드리뷰**: superpowers:requesting-code-review로 워크트리 전체 diff 리뷰
2. **master 미커밋 변경 선커밋 (머지 전 필수)**: master에서 공유링크 API 서버화·RLS 변경을 별도 커밋 — `supabase/.temp/` 제외, 메시지 `feat: 공유링크·댓글 API 서버화 및 RLS 강화`. **이 커밋 전 머지 금지.**
3. **머지·정리**: superpowers:finishing-a-development-branch — feature 브랜치를 master에 머지, 워크트리 정리
4. **배포**: `git fetch` → `git log HEAD..origin/master`로 원격 분기 확인 → push → Vercel `Ready` 폴링 → `policy-analytics-engine.vercel.app` 200 확인
5. **프로덕션 스모크 5항목** (배포 URL 기준):
   - 상관 히트맵·산점도 표시
   - 그룹비교 오차막대 + 효과크기 문구
   - 시계열 MoM/YoY 표시
   - 보고서 신규 섹션 + 인쇄 미리보기
   - AI 설명에 유의성·효과크기 언급
6. **마무리**: agents done 체인 역순 (담당 done → lead-dev review·done → orchestrator review·done) + 위키 쓰기 루프 (새 패턴을 `분석툴` 관련 노트에 반영)

---

## 보류 과제 (이번 범위 밖 — 착수 전 사용자 확인 필요)

| 과제 | 사유 |
|---|---|
| PPTX 내보내기 | pptxgenjs 의존성 추가 필요 — 신규 의존성 0 원칙 위배, **사용자 승인 필요** |
| HWPX 내보내기 | 브라우저에서 실현 불가 — 별도 Python 파이프라인으로 분리 검토 |
| 계절성·STL 분해 | 설계 문서에서 명시적 범위 제외 |
| 행 수 상한·샘플링 정책 | Phase 5 성능 측정 결과 확인 후 결정 |
