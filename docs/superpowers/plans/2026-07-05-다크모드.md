# 다크모드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 정책 분석 엔진(`C:\업무\분석툴`)에 라이트/다크/시스템 3단 전환 다크모드를 추가한다. `@astryxdesign/theme-neutral`이 이미 보유한 다크 토큰을 `@astryxdesign/core`의 `Theme` `mode` prop으로 실제 배선하고, 사이드 내비게이션에 순환 토글을 붙인다.

**Architecture:** 모드 계산 로직(순환·localStorage 읽기)은 순수 함수로 `lib/theme/themeMode.ts`에 분리해 기존 vitest 설정(`lib/**/*.test.ts`, node 환경)으로 그대로 테스트한다. `components/AppTheme.tsx`는 이 순수 함수를 사용하는 상태 보유 클라이언트 컴포넌트로 확장되고, `React.Context`로 `{mode, cycleMode}`를 하위 트리(`AppNavShell`)에 노출한다. `app/layout.tsx`는 정적 `data-theme="light"`를 제거하고 하이드레이션 이전 동기 실행되는 인라인 스크립트로 대체해 FOUC를 막는다.

**Tech Stack:** Next.js 14 App Router, React Context, `@astryxdesign/core` (`Theme`, `SideNavItem`, `SideNavSection`), `@astryxdesign/theme-neutral` (`neutralTheme`), `lucide-react` (`Sun`/`Moon`/`Monitor`), Vitest (node 환경, 기존 `lib/**/*.test.ts` 설정 재사용).

---

## Task 1: 모드 순환 순수 로직 (`lib/theme/themeMode.ts`)

**Files:**
- Create: `lib/theme/themeMode.ts`
- Test: `lib/theme/themeMode.test.ts`

- [ ] **Step 1: Write the failing test**

```ts
// lib/theme/themeMode.test.ts
import { describe, expect, it } from "vitest";
import {
  THEME_MODE_STORAGE_KEY,
  cycleThemeMode,
  readStoredThemeMode,
  type ThemeModeSetting,
} from "@/lib/theme/themeMode";

function fakeStorage(initial: Record<string, string> = {}): Storage {
  const store = { ...initial };
  return {
    getItem: (key: string) => (key in store ? store[key] : null),
    setItem: (key: string, value: string) => {
      store[key] = value;
    },
    removeItem: (key: string) => {
      delete store[key];
    },
    clear: () => {
      for (const key of Object.keys(store)) delete store[key];
    },
    key: (index: number) => Object.keys(store)[index] ?? null,
    get length() {
      return Object.keys(store).length;
    },
  } as Storage;
}

describe("cycleThemeMode", () => {
  it("cycles light -> dark -> system -> light", () => {
    expect(cycleThemeMode("light")).toBe("dark");
    expect(cycleThemeMode("dark")).toBe("system");
    expect(cycleThemeMode("system")).toBe("light");
  });
});

describe("readStoredThemeMode", () => {
  it("defaults to system when nothing is stored", () => {
    const mode = readStoredThemeMode(fakeStorage());
    expect(mode).toBe("system");
  });

  it("returns the stored value when it is a valid mode", () => {
    const storage = fakeStorage({ [THEME_MODE_STORAGE_KEY]: "dark" });
    expect(readStoredThemeMode(storage)).toBe("dark");
  });

  it("falls back to system when the stored value is invalid", () => {
    const storage = fakeStorage({ [THEME_MODE_STORAGE_KEY]: "purple" });
    expect(readStoredThemeMode(storage)).toBe("system");
  });
});

it("type export compiles", () => {
  const mode: ThemeModeSetting = "system";
  expect(mode).toBe("system");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/theme/themeMode.test.ts`
Expected: FAIL with `Cannot find module '@/lib/theme/themeMode'` (file does not exist yet)

- [ ] **Step 3: Write minimal implementation**

```ts
// lib/theme/themeMode.ts
export type ThemeModeSetting = "light" | "dark" | "system";

export const THEME_MODE_STORAGE_KEY = "pae-theme-mode";

const MODE_ORDER: readonly ThemeModeSetting[] = ["light", "dark", "system"];

export function isThemeModeSetting(value: string | null): value is ThemeModeSetting {
  return value === "light" || value === "dark" || value === "system";
}

export function cycleThemeMode(current: ThemeModeSetting): ThemeModeSetting {
  const currentIndex = MODE_ORDER.indexOf(current);
  const nextIndex = (currentIndex + 1) % MODE_ORDER.length;
  return MODE_ORDER[nextIndex];
}

export function readStoredThemeMode(storage: Pick<Storage, "getItem">): ThemeModeSetting {
  const stored = storage.getItem(THEME_MODE_STORAGE_KEY);
  return isThemeModeSetting(stored) ? stored : "system";
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/theme/themeMode.test.ts`
Expected: PASS (6 tests)

- [ ] **Step 5: Commit**

```bash
git add lib/theme/themeMode.ts lib/theme/themeMode.test.ts
git commit -m "feat: add pure theme mode cycling and storage logic"
```

---

## Task 2: `AppTheme`를 상태 보유 컴포넌트로 확장 + `ThemeModeContext`

**Files:**
- Modify: `components/AppTheme.tsx`

- [ ] **Step 1: Write the failing test**

이 단계는 React 컴포넌트이며 프로젝트에 `jsdom`/`@testing-library/react`가 설치되어 있지 않다(`vitest.config.ts`가 `environment: "node"`, `include: ["lib/**/*.test.ts"]`로 한정됨 — Task 1에서 확인). 컴포넌트 자체는 Task 1에서 이미 순수 로직으로 분리해 테스트했으므로, 이 태스크는 그 로직을 컴포넌트에 배선하는 것으로 신규 테스트 인프라 도입 없이 진행한다(YAGNI). 대신 빌드 타입체크로 계약을 검증한다.

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 아래 Step 3 구현 전에는 `components/AppTheme.tsx`가 아직 `mode`를 참조하지 않으므로 에러 없이 통과(현재 상태 기준선 확인용). 이 커맨드 결과를 기록해두고 Step 4에서 다시 실행해 여전히 통과하는지 비교한다.

- [ ] **Step 2: (해당 없음 — Step 1이 기준선 확인으로 대체됨, 코드 변경 없음)**

- [ ] **Step 3: 구현**

```tsx
// components/AppTheme.tsx
"use client";
// Astryx 테마 프로바이더 클라이언트 경계. neutralTheme이 client-only 모듈(defineSyntaxTheme)을
// 모듈 스코프에서 직접 호출하므로, 서버 컴포넌트(layout.tsx)에서 바로 임포트하면 RSC 경계 오류가 난다.
import { createContext, useContext, useState, type ReactNode } from "react";
import { Theme } from "@astryxdesign/core/theme";
import { neutralTheme } from "@astryxdesign/theme-neutral";
import {
  THEME_MODE_STORAGE_KEY,
  cycleThemeMode,
  readStoredThemeMode,
  type ThemeModeSetting,
} from "@/lib/theme/themeMode";

interface ThemeModeContextValue {
  mode: ThemeModeSetting;
  cycleMode: () => void;
}

const ThemeModeContext = createContext<ThemeModeContextValue>({
  mode: "system",
  cycleMode: () => {},
});

export function useThemeMode(): ThemeModeContextValue {
  return useContext(ThemeModeContext);
}

function getInitialMode(): ThemeModeSetting {
  if (typeof window === "undefined") return "system";
  try {
    return readStoredThemeMode(window.localStorage);
  } catch {
    return "system";
  }
}

export function AppTheme({ children }: { children: ReactNode }) {
  const [mode, setMode] = useState<ThemeModeSetting>(getInitialMode);

  function cycleMode() {
    setMode((current) => {
      const next = cycleThemeMode(current);
      try {
        window.localStorage.setItem(THEME_MODE_STORAGE_KEY, next);
      } catch {
        // localStorage 접근 불가(프라이빗 모드 등) — 상태는 갱신하되 영속화만 건너뜀
      }
      return next;
    });
  }

  return (
    <ThemeModeContext.Provider value={{ mode, cycleMode }}>
      <Theme theme={neutralTheme} mode={mode}>
        {children}
      </Theme>
    </ThemeModeContext.Provider>
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: PASS (에러 없음 — `Theme`의 `mode` prop 타입이 `ThemeModeSetting`과 호환되는 `'light'|'dark'|'system'`임을 확인, `node_modules/@astryxdesign/core/src/theme/Theme.tsx`에서 이미 확인된 시그니처)

Run: `npx vitest run lib/theme/themeMode.test.ts`
Expected: PASS (6 tests, Task 1과 동일 — 회귀 없음 확인)

- [ ] **Step 5: Commit**

```bash
git add components/AppTheme.tsx
git commit -m "feat: wire ThemeModeContext and stateful mode into AppTheme"
```

---

## Task 3: FOUC 방지 인라인 스크립트 (`app/layout.tsx`)

**Files:**
- Modify: `app/layout.tsx`

- [ ] **Step 1: 현재 상태 확인 (기준선)**

Run: `curl -s http://localhost:3000 | grep -o 'data-theme="[^"]*"'` (단, 이 단계는 Task 완료 후 Step 4에서 실행 — 지금은 코드 확인만)

현재 `app/layout.tsx`는 다음과 같이 정적으로 고정되어 있음(수정 대상 확인):

```tsx
<html lang="ko" data-theme="light">
```

- [ ] **Step 2: (해당 없음 — 정적 마크업 diff이므로 별도 실행 검증은 Step 4로 이동)**

- [ ] **Step 3: 구현**

```tsx
// app/layout.tsx
// 루트 레이아웃: Astryx 테마 프로바이더를 최상단에서 1회만 적용한다
import type { Metadata } from "next";
import { AppTheme } from "@/components/AppTheme";
import "./globals.css";

export const metadata: Metadata = {
  title: "Policy Analytics Engine",
  description: "AI가 분석하는 서비스가 아니라, 분석 엔진이 계산하고 AI가 설명하는 공공데이터 분석 플랫폼",
};

const THEME_INIT_SCRIPT = `
(function () {
  try {
    var m = localStorage.getItem('pae-theme-mode');
    var dark = m === 'dark' || (m !== 'light' && window.matchMedia('(prefers-color-scheme: dark)').matches);
    document.documentElement.setAttribute('data-theme', dark ? 'dark' : 'light');
  } catch (e) {}
})();
`;

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ko">
      <head>
        <script dangerouslySetInnerHTML={{ __html: THEME_INIT_SCRIPT }} />
      </head>
      <body>
        <AppTheme>{children}</AppTheme>
      </body>
    </html>
  );
}
```

`'pae-theme-mode'` 리터럴은 `lib/theme/themeMode.ts`의 `THEME_MODE_STORAGE_KEY`와 반드시 동일해야 한다(하이드레이션 전 순수 인라인 스크립트는 모듈을 import할 수 없으므로 문자열 리터럴 복제가 불가피 — 이 계약이 깨지면 Task 5의 수동 검증에서 FOUC로 드러난다).

- [ ] **Step 4: 검증**

Run: `npm run build && npm run start`
Run(별도 터미널): `curl -s http://localhost:3000/dashboard | grep -o '<html[^>]*>'`
Expected: `<html lang="ko">` (더 이상 `data-theme="light"`가 정적으로 박혀있지 않음 — 스크립트가 클라이언트에서 동적으로 설정하므로 서버 HTML에는 없는 것이 정상)

Run(브라우저 devtools 또는 Playwright): 페이지 로드 후 `document.documentElement.getAttribute('data-theme')`
Expected: `"light"` 또는 `"dark"` (시스템 설정에 따름, `localStorage`가 비어있는 최초 방문 기준)

- [ ] **Step 5: Commit**

```bash
git add app/layout.tsx
git commit -m "feat: replace static light theme with FOUC-safe inline init script"
```

---

## Task 4: 사이드 내비 토글 UI (`components/layout/AppNavShell.tsx`)

**Files:**
- Modify: `components/layout/AppNavShell.tsx`

- [ ] **Step 1: 현재 구조 확인 (기준선)**

`components/layout/AppNavShell.tsx`는 현재 `"엔진"` `SideNavSection` 다음에 아무 항목도 없음(파일 끝까지 82줄). 이 태스크는 그 뒤에 새 섹션을 추가한다.

- [ ] **Step 2: (해당 없음 — Step 1이 구조 확인으로 대체됨)**

- [ ] **Step 3: 구현**

```tsx
// components/layout/AppNavShell.tsx
"use client";
// 전체 화면 공통 뼈대: Astryx AppShell 위에 좌측 메뉴/상단바/데모 모드 배지를 고정 배치한다
import Link from "next/link";
import { usePathname } from "next/navigation";
import {
  AppShell,
  SideNav,
  SideNavHeading,
  SideNavSection,
  SideNavItem,
  TopNav,
  TopNavHeading,
  Badge,
} from "@astryxdesign/core";
import {
  LayoutDashboard,
  FolderKanban,
  UploadCloud,
  BarChart3,
  Sparkles,
  BookOpen,
  Sun,
  Moon,
  Monitor,
} from "lucide-react";
import { isDemoMode } from "@/lib/data/store";
import { useThemeMode } from "@/components/AppTheme";

const NAV_ITEMS = [
  { href: "/dashboard", label: "대시보드", icon: LayoutDashboard },
  { href: "/upload", label: "새 프로젝트 업로드", icon: UploadCloud },
  { href: "/manual", label: "매뉴얼", icon: BookOpen },
];

const THEME_MODE_LABEL: Record<"light" | "dark" | "system", string> = {
  light: "테마: 라이트",
  dark: "테마: 다크",
  system: "테마: 시스템 설정",
};

const THEME_MODE_ICON = {
  light: Sun,
  dark: Moon,
  system: Monitor,
} as const;

export function AppNavShell({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();
  const { mode, cycleMode } = useThemeMode();
  const ThemeModeIcon = THEME_MODE_ICON[mode];

  return (
    <AppShell
      topNav={
        <TopNav
          label="정책 분석 엔진 내비게이션"
          heading={
            <TopNavHeading
              logo={<Sparkles size={20} />}
              heading="정책 분석 엔진"
              headingHref="/dashboard"
            />
          }
          endContent={isDemoMode ? <Badge variant="info" label="데모 모드" /> : undefined}
        />
      }
      sideNav={
        <SideNav
          header={
            <SideNavHeading
              icon={<FolderKanban size={20} />}
              heading="정책 분석 엔진"
              headingHref="/dashboard"
            />
          }
        >
          <SideNavSection title="메뉴">
            {NAV_ITEMS.map((item) => (
              <SideNavItem
                key={item.href}
                as={Link}
                href={item.href}
                label={item.label}
                icon={<item.icon size={18} />}
                isSelected={pathname === item.href || pathname.startsWith(`${item.href}/`)}
              />
            ))}
          </SideNavSection>
          <SideNavSection title="엔진">
            <SideNavItem label="규칙 기반 분석 엔진" icon={<BarChart3 size={18} />} isDisabled />
          </SideNavSection>
          <SideNavSection title="설정">
            <SideNavItem
              label={THEME_MODE_LABEL[mode]}
              icon={<ThemeModeIcon size={18} />}
              onClick={cycleMode}
            />
          </SideNavSection>
        </SideNav>
      }
    >
      {children}
    </AppShell>
  );
}
```

`useThemeMode`를 `components/AppTheme.tsx`에서 export하도록 Task 2에서 이미 반영했으므로 추가 export 작업은 불필요하다. `SideNavItem`은 `href` 없이 `onClick`만 주어지면 `<button>`으로 렌더링됨(`node_modules/@astryxdesign/core/src/SideNav/SideNavItem.tsx` 확인됨) — 별도 `Tooltip` 래핑 없이 `label` 텍스트 자체가 현재 모드를 always-visible로 보여주므로 설계 문서의 "Tooltip으로 레이블 표시"보다 단순하면서 동일한 목적(현재 모드 인지)을 달성한다(YAGNI — 기존 다른 항목들도 label 텍스트만 사용).

- [ ] **Step 4: 검증**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: PASS

Run: `npm run build`
Expected: PASS (빌드 성공, 타입 에러 없음)

- [ ] **Step 5: Commit**

```bash
git add components/layout/AppNavShell.tsx
git commit -m "feat: add theme mode toggle to side navigation"
```

---

## Task 5: 전체 검증 + 수동 브라우저 확인 + 배포

**Files:** 없음(검증·배포 전용 태스크)

- [ ] **Step 1: 자동화 테스트 전체 실행**

Run: `npx vitest run`
Expected: 기존 테스트 전부 + Task 1의 `lib/theme/themeMode.test.ts` 6건 포함 PASS

Run: `npm run build`
Expected: PASS

- [ ] **Step 2: 수동 Playwright 검증 (필수 — 자동화만으로 완료 선언 금지)**

이유: [[분석툴-전처리-샘플데이터]]에 기록된 교훈 — 시각 기능은 자동화 테스트 통과만으로 완료 선언하지 않는다.

```bash
npm run dev
```

Playwright CLI로:
```bash
"$PWCLI" open http://localhost:3000/dashboard --headed
"$PWCLI" snapshot
```
→ 스냅샷에서 사이드 내비 "설정" 그룹, "테마: 시스템 설정" (또는 현재 시스템 설정에 따른 라벨) 항목 확인

```bash
"$PWCLI" click <테마 토글 항목의 ref>
"$PWCLI" screenshot
```
Expected: 스크린샷에서 배경/텍스트 색상이 실제로 다크 톤(예: `neutralTheme`의 어두운 배경 토큰)으로 전환됨을 육안 확인. 한 번 더 클릭해 라이트로 순환 복귀하는 것도 확인.

- [ ] **Step 3: Push + 배포**

```bash
git fetch origin
git log HEAD..origin/master --oneline
```
Expected: 빈 출력(원격 분기 없음). 출력이 있으면 먼저 병합 후 진행.

```bash
git push origin master
```

```bash
npx vercel ls --yes
```
→ 최신 배포가 `Ready` 상태가 될 때까지 확인(폴링)

```bash
npx vercel alias ls
```
→ production alias 도메인(`policy-analytics-engine.vercel.app`) 확인

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://policy-analytics-engine.vercel.app/dashboard
```
Expected: `200`

- [ ] **Step 4: 완료 기록**

Run:
```bash
python "C:/업무/agents/scripts/update_status.py" frontend done "다크모드 구현 완료(순환토글+FOUC방지+neutralTheme 다크토큰 배선), production 배포 확인"
```

---

## Self-Review (완료됨)

**1. 스펙 커버리지** — `2026-07-05-다크모드-design.md` 대조:
- 모드 상태 관리(3단 순환) → Task 1, 2 ✅
- localStorage 영속화(`pae-theme-mode`) → Task 1, 2 ✅
- FOUC 방지 → Task 3 ✅
- 사이드 내비 토글 UI → Task 4 ✅ (단, Tooltip 대신 label 텍스트로 단순화 — Task 4에 사유 명시)
- 에러 처리(localStorage 접근 불가·matchMedia 미지원) → Task 2의 try/catch, Task 3의 인라인 스크립트 try/catch ✅
- 테스트(순환 로직 단위 + 수동 Playwright) → Task 1, Task 5 Step 2 ✅
- 배포 절차 → Task 5 Step 3 ✅

**2. 플레이스홀더 스캔** — "TBD"/"적절히 처리"/"위와 유사" 패턴 없음. 모든 스텝에 실행 가능한 전체 코드 포함.

**3. 타입/네이밍 일관성** — `ThemeModeSetting`, `THEME_MODE_STORAGE_KEY`, `cycleThemeMode`, `readStoredThemeMode`, `cycleMode`, `useThemeMode`가 Task 1~4 전체에서 동일한 이름·시그니처로 사용됨. `mode` prop은 Task 2에서 `Theme`에, Task 4에서 `useThemeMode()` 반환값으로 일관되게 소비됨.

**변경 사항 1건**: 설계 문서의 "Astryx Tooltip으로 레이블 표시"를 "SideNavItem label 텍스트로 상시 표시"로 단순화(Task 4에 사유 기재) — Tooltip 컴포넌트의 정확한 prop API를 확인하지 않고 도입하면 스펙 위반(플레이스홀더성 가정 코드)이 될 위험이 있었고, 기존 내비 항목들도 hover tooltip 없이 label만 쓰는 패턴과 일치시켜 시각 언어 일관성을 유지했다.
